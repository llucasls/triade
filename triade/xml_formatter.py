import xml.etree.ElementTree as ET

from element import Element


class XML:
    @staticmethod
    def loads(input_data: str) -> object:
        "Parse XML string into Python object"
        pass

    @staticmethod
    def dumps(input_data: object) -> str:
        "Converts Python object into XML string"
        pass
